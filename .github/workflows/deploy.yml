name: Deploy to FTP

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Deploy to FTP
        uses: SamKirkland/FTP-Deploy-Action@v4
        with:
          server: ftp.waqup.space
          username: waqup-website@waqup.space
          password: &j6yV5#LoDNM
          server-dir: /public_html/
          local-dir: ./
          dangerous-clean-slate: true 